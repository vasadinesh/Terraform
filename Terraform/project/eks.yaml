apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: project
  region: us-east-1

managedNodeGroups:
  - name: project
    instanceTypes: ["m5.large", "c3.large", "c4.large", "c5.large"]
    desiredCapacity: 3
    spot: true